# THIS FILE IS GENERATED WITH 'make generate' - DO NOT EDIT MANUALLY
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    application.giantswarm.io/team: honeybadger
    rbac.kyverno.io/aggregate-to-admission-controller: "true"
    rbac.kyverno.io/aggregate-to-background-controller: "true"
    rbac.kyverno.io/aggregate-to-reports-controller: "true"
  name: kyverno:gs-ux:prepend-cluster-app-config-map
rules:
  - apiGroups:
      - application.giantswarm.io
    resources:
      - apps
    verbs:
      - get
      - list
      - watch
      - update
---
apiVersion: policies.kyverno.io/v1
kind: MutatingPolicy
metadata:
  name: prepend-cluster-app-config-map
  labels:
    application.giantswarm.io/team: honeybadger
  annotations:
    policies.kyverno.io/title:
      Prepend 'cluster-app-installation-values' ConfigMap to App CR extraConfigs for cluster-* charts
    policies.kyverno.io/subject: App
    policies.kyverno.io/description: >
      Checks if the 'cluster-app-installation-values' ConfigMap is in the extraConfigs list of a cluster-* App
      CR, and if not, prepends it as the first entry.
spec:
  # failurePolicy: Ignore so a mutation/evaluation error never blocks App admission;
  # the entry is still backfilled later via mutateExisting / background.
  failurePolicy: Ignore
  evaluation:
    # admission: mutate new/updated Apps inline at admission time.
    admission:
      enabled: true
    # background + mutateExisting: backfill pre-existing Apps. mutateExisting is
    # event-driven — when any matching App is created/updated, all existing
    # matching Apps are (re)evaluated and backfilled. There is no Kyverno-1.17
    # equivalent of the old ClusterPolicy `mutateExistingOnPolicyUpdate`
    # immediate-on-install backfill.
    background:
      enabled: true
    mutateExisting:
      enabled: true
  matchConstraints:
    resourceRules:
      - apiGroups:
          - application.giantswarm.io
        apiVersions:
          - "*"
        operations:
          - CREATE
          - UPDATE
        resources:
          - apps
  matchConditions:
    # Only cluster-* provider charts.
    - name: cluster-app-name
      expression: >-
        has(object.spec) && has(object.spec.name) &&
        object.spec.name in ['cluster-aws', 'cluster-azure', 'cluster-cloud-director', 'cluster-vsphere', 'cluster-eks']
    # Skip if the ConfigMap entry is already present (idempotent, no duplication).
    - name: configmap-not-present
      expression: >-
        !has(object.spec.extraConfigs) ||
        !object.spec.extraConfigs.exists(e, has(e.name) && e.name == 'cluster-app-installation-values')
  variables:
    # The entry to prepend. dyn() forces a map(string, dyn) so the heterogeneous
    # values (string namespace from the object + int priority) compile under CEL.
    - name: entry
      expression: >-
        {"kind": dyn("configMap"), "name": dyn("cluster-app-installation-values"), "namespace": dyn(object.metadata.namespace), "priority": dyn(10)}
  mutations:
    - patchType: JSONPatch
      jsonPatch:
        # When extraConfigs already exists (incl. empty []), prepend at index 0.
        # When it is absent, create the list with the entry as the only element —
        # a JSON Patch add at a numeric index requires the parent array to exist.
        expression: >-
          has(object.spec.extraConfigs)
          ? [JSONPatch{op: "add", path: "/spec/extraConfigs/0", value: variables.entry}]
          : [JSONPatch{op: "add", path: "/spec/extraConfigs", value: [variables.entry]}]
