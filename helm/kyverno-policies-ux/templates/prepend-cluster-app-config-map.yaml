# THIS FILE IS GENERATED WITH 'make generate' - DO NOT EDIT MANUALLY
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    application.giantswarm.io/team: honeybadger
    rbac.kyverno.io/aggregate-to-admission-controller: "true"
    rbac.kyverno.io/aggregate-to-background-controller: "true"
    rbac.kyverno.io/aggregate-to-reports-controller: "true"
  name: kyverno:gs-ux:prepend-cluster-app-config-map
rules:
  - apiGroups:
      - application.giantswarm.io
    resources:
      - apps
    verbs:
      - get
      - list
      - watch
      - update
      - view
---
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: prepend-cluster-app-config-map
  labels:
    application.giantswarm.io/team: honeybadger
  annotations:
    policies.kyverno.io/title:
      Prepend 'cluster-app-installation-values' ConfigMap to App CR extraConfigs for cluster-* charts
    policies.kyverno.io/subject: App
    policies.kyverno.io/description: >
      Checks if the 'cluster-app-installation-values' ConfigMap is in the extraConfigs list of a cluster-* App
      CR, and if not, prepends it as the first entry.
spec:
  background: true
  rules:
    # Rule 1: extraConfigs is absent (null) — create the field with the entry as the only element.
    # JSON Patch op:add at a numeric index requires the parent array to already exist, so we
    # must initialise it in a separate rule rather than patching at /spec/extraConfigs/0.
    - name: init-extraconfigs-list
      match:
        any:
          - resources:
              kinds:
                - App
      # Also run during periodic background scans so pre-existing Apps without
      # extraConfigs are backfilled automatically (no manual touch required).
      skipBackgroundRequests: false
      preconditions:
        all:
          - key: "{{ `{{` }} request.object.spec.name {{ `}}` }}"
            operator: AnyIn
            value:
              - cluster-aws
              - cluster-azure
              - cluster-cloud-director
              - cluster-vsphere
              - cluster-eks
          - key: "{{ `{{` }} request.object.spec.extraConfigs | to_string(@) {{ `}}` }}"
            operator: Equals
            value: "null"
      mutate:
        patchesJson6902: |-
          - op: add
            path: /spec/extraConfigs
            value:
              - kind: configMap
                name: cluster-app-installation-values
                namespace: "{{ `{{` }} request.object.metadata.namespace {{ `}}` }}"
                priority: 10

    # Rule 2: extraConfigs exists (possibly empty []) but does not yet contain the entry — prepend.
    - name: prepend-to-extraconfigs-list
      match:
        any:
          - resources:
              kinds:
                - App
      # Also run during periodic background scans to backfill existing Apps.
      skipBackgroundRequests: false
      preconditions:
        all:
          - key: "{{ `{{` }} request.object.spec.name {{ `}}` }}"
            operator: AnyIn
            value:
              - cluster-aws
              - cluster-azure
              - cluster-cloud-director
              - cluster-vsphere
              - cluster-eks
          - key: "{{ `{{` }} request.object.spec.extraConfigs | to_string(@) {{ `}}` }}"
            operator: NotEquals
            value: "null"
          - key:
              "{{ `{{` }} request.object.spec.extraConfigs | [?name=='cluster-app-installation-values'] | length(@) {{ `}}` }}"
            operator: Equals
            value: "0"
      mutate:
        patchesJson6902: |-
          - op: add
            # The path /0 instructs Kyverno to insert the value at index 0 (prepend).
            path: /spec/extraConfigs/0
            value:
              kind: configMap
              name: cluster-app-installation-values
              namespace: "{{ `{{` }} request.object.metadata.namespace {{ `}}` }}"
              priority: 10
